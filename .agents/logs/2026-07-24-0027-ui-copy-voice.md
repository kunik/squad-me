# UI copy voice rule + rewrite

## Summary
Added UA-first product-voice Cursor rule and rewrote related i18n strings
(clear-sessions, reset warnings, several auth errors) to plain speech with
takeaway parity + functional accuracy gates. Revoke-others API work was already
committed separately (`3f680ca`).

## Key decisions
- Prefer devices/sign-in language over “sessions” in user-facing copy.
- Required checks: takeaway parity (OLD vs NEW) then behavior vs code/docs.
- Commit only voice rule + `i18n.ts`; leave other WIP unstaged.

## Files changed
- `.cursor/rules/ui-copy-voice.mdc` (new)
- `src/client/i18n.ts` (UA + EN string rewrites)

## Verification
- Diff review vs revoke-others behavior (current stays; others sign out)
- `npx tsc --noEmit -p tsconfig.json` (clean)
- `npx vitest run src/worker/identity/routes.test.ts` — 28 passed

## Pending
- None for this commit.
