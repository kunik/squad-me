# Project Notes

Store concise operational facts that are useful in future sessions but cannot
be derived reliably from source code, documentation, or git history.

<!-- Add entries below. -->

- Cloudflare account Taras (`2758c21b02e5c7efcfa745cb49948ace`); use
  `npx wrangler`. Full infra steps: `docs/provision.md`. Cloud Dev resources
  provisioned (D1/R2/Queues); see `docs/inventory-dev.md`. Still open: DNS
  `dev.squadme.app`, Access, GitHub Environments `cloud-dev`/`production`,
  first `npm run deploy:dev`.
